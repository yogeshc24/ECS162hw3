<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">   
    
    <!-- font awesome for the trashcan icon --> 
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">    
    
    <title>{{{appName}}}</title>

    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="logo">
            <img src="images/logo.png "  class="header-logo">
            <h1>{{{appName}}}</h1>
        </div>
        <nav>
            <ul>
                <!-- conditional rendering based on variables - Is the user logged in?-->
                {{#if loggedIn}}
                    <li><a href="/">Home</a></li>
                    <li><a href="/profile">Profile</a></li>
                    <li><a href="/logout">Logout <span>{{user.username}}</span> </a></li>
                    <div class="user-info">

                        <!-- conditional rendering based on variables -->
                        <img src="{{#if user.avatar_url}}
                                        {{user.avatar_url}}
                                    {{else}}
                                        /avatar/{{user.username}}
                                    {{/if}}" 
                                    alt="User Avatar" 
                                    class="header-avatar">
                    </div>
                {{else}}
                    <li><a href="/">Home</a></li>
                    <li><a href="/login">Login/Register</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    {{{body}}}

    <footer>
        <p>© {{{copyrightYear}}} {{{appName}}}. All rights reserved.</p>
    </footer>
</body>
</html>


<style>
 
body, html {
    margin: 0;
    padding: 0;
    font-family: 'Arial', sans-serif;
    background-color: #d4cdcd; 
    color: #000000; 
}
/* Header styles */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background: linear-gradient(45deg, #1e1e1e, #222); /* Gradient background */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.6);
    border-bottom: 1px solid #444;
}

header .logo {
    display: flex;
    align-items: center;
}

header .logo img.header-logo {
    width: 80px;
    height: 80px;
    margin-right: 10px;
}

header h1 {
    font-size: 24px;
    color: #ffd700; /* Gold color */
    text-shadow: 0 0 5px #fff, 0 0 10px #ffd700, 0 0 15px #ffd700, 0 0 20px #ffd700, 0 0 25px #ffd700;
}

nav ul {
    list-style: none;
    display: flex;
    margin: 0;
    padding: 0;
}

nav ul li {
    margin: 0 10px;
}

nav ul li a {
    text-decoration: none;
    color: #fff;
    font-size: 18px;
    transition: color 0.3s ease-in-out;
}

nav ul li a:hover {
    color: #ffd700; /* Gold color on hover */
}

.user-info {
    display: flex;
    align-items: center;
    margin-left: 20px;
}

.header-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    margin-left: 10px;
    border: 2px solid #ffd700; /* Gold border */
}

/* Footer styles */
footer {
    text-align: center;
    padding: 20px 0;
    background: linear-gradient(45deg, #222, #1e1e1e); /* Gradient background */
    border-top: 1px solid #444;
    box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.6);
}

footer p {
    margin: 0;
    color: #ffd700; /* Gold color */
}

/* Starry background effect */
body::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('https://www.transparenttextures.com/patterns/stardust.png'); /* Starry pattern */
    opacity: 0.1; /* Make stars subtle */
    pointer-events: none; /* Allow interaction with underlying elements */
}

/* Additional body styling to handle flex */
body {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

header, footer {
    flex: 0 0 auto;
}

main {
    flex: 1 0 auto;
    padding: 20px;
}

</style>